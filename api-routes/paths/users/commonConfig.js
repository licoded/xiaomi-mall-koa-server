const UserController = require('../../../app/controllers/userController');
const Utils = require('../../utils');
const tags = ['UserController'];

module.exports = {
  tags,
  Utils,
  UserController,
}